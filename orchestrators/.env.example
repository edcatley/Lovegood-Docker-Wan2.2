# Mock API config
MOCK_API_KEY=test-key
MOCK_IMAGE=lovegood-comfyui:latest
MOCK_API_PORT=9000

# Path to your models on the host, mounted at /workspace inside the container
MODELS_PATH=

# Sidecar port inside the container
SIDECAR_PORT=8189
